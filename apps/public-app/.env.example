# Used by server components (SSR fetches happen inside the Docker/server network)
GATEWAY_URL=http://localhost:8080

# Used by client components (inquiry form) - must be reachable from the browser
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8080

NEXT_PUBLIC_SITE_URL=http://localhost:3000
